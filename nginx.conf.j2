server {
    listen 80;
    server_name your_domain.com;

    location / {
        root /var/www/client_app/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Add other Nginx configurations as needed
}